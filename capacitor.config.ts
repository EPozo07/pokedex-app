import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.eloy.pokedexapp',
  appName: 'pokedexApp',
  webDir: 'dist/pokedex-app/browser'
};

export default config;